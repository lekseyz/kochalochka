<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Регистрация</title>
  <!-- Подключение Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white text-center">
          <h4>Регистрация</h4>
        </div>
        <div class="card-body">
          <% if @user.errors.any? %>
            <div class="alert alert-danger">
              <h4>Ошибки при регистрации:</h4>
              <ul>
                <% @user.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <%= form_with model: @user, local: true do |form| %>
            <div class="mb-3">
              <%= form.label :username, "Имя пользователя", class: "form-label" %>
              <%= form.text_field :username, class: "form-control", placeholder: "Введите имя пользователя", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :password, "Пароль", class: "form-label" %>
              <%= form.password_field :password, class: "form-control", placeholder: "Введите пароль", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :password_confirmation, "Подтверждение пароля", class: "form-label" %>
              <%= form.password_field :password_confirmation, class: "form-control", placeholder: "Подтвердите пароль", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :weight, "Вес (кг)", class: "form-label" %>
              <%= form.number_field :weight, class: "form-control", placeholder: "Введите ваш вес", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :height, "Рост (см)", class: "form-label" %>
              <%= form.number_field :height, class: "form-control", placeholder: "Введите ваш рост", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :birth_day, "Дата рождения", class: "form-label" %>
              <%= form.date_field :birth_day, class: "form-control", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :goal, "Цель", class: "form-label" %>
              <%= form.select :goal, ['Похудение', 'Набор массы', 'Поддержание формы'], { prompt: 'Выберите вашу цель' }, class: "form-select", required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :sex, "Пол", class: "form-label" %>
              <div class="form-check form-check-inline">
                <%= form.radio_button :sex, 'Мужской', class: "form-check-input" %>
                <%= form.label :sex, 'Мужской', class: "form-check-label" %>
              </div>
              <div class="form-check form-check-inline">
                <%= form.radio_button :sex, 'Женский', class: "form-check-input" %>
                <%= form.label :sex, 'Женский', class: "form-check-label" %>
              </div>
            </div>
            <%= form.submit 'Зарегистрироваться', class: 'btn btn-primary w-100' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Подключение Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
