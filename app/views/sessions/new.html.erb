<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Вход</title>
  <!-- Подключение Bootstrap через CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
      body {
          background-color: #f0f2f5;
          font-family: 'Arial', sans-serif;
      }
      .login-container {
          margin-top: 100px;
      }
      .card {
          border-radius: 15px;
      }
      .card-header {
          background-color: #007bff;
          color: white;
          border-radius: 15px 15px 0 0;
      }
      .form-control {
          border-radius: 10px;
      }
  </style>
</head>
<body>

<div class="container login-container">
  <div class="row justify-content-center">
    <div class="col-md-4">
      <div class="card shadow-lg">
        <div class="card-header text-center">
          <h4>Вход в систему</h4>
        </div>
        <div class="card-body">
          <%= form_with url: session_path, method: :post, local: true do |form| %>
            <div class="mb-3">
              <%= form.label :username, 'Имя пользователя', class: 'form-label' %>
              <%= form.text_field :username, class: 'form-control', placeholder: 'Введите имя пользователя', required: true %>
            </div>
            <div class="mb-3">
              <%= form.label :password, 'Пароль', class: 'form-label' %>
              <%= form.password_field :password, class: 'form-control', placeholder: 'Введите пароль', required: true %>
            </div>
            <div class="d-flex justify-content-between">
            </div>
            <button type="submit" class="btn btn-primary w-100 mt-3">Войти</button>
          <% end %>
        </div>
        <div class="card-footer text-center">
          <p>Нет аккаунта? <%= link_to 'Зарегистрироваться', new_user_path %></p>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Подключение Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
